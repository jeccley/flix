<h1><%= pluralize(@users.size, "User") %></h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= user.name %> &bull;
      <%= link_to user.username, user %> &bull;
      created
      <%= time_ago_in_words(user.created_at) %> ago
    </li>
  <% end %>
</ul>